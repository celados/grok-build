id: skill-fuzzy-dedup-remove-test-dedupe_rekeys_every_same_scope_claimant
language: Rust
severity: error
message: Remove a test coupled to deleted name-shadowing semantics.
rule:
  kind: function_item
  regex: "fn dedupe_rekeys_every_same_scope_claimant"
fix: |-
  fn dedupe_rekeys_every_same_scope_claimant() {}
